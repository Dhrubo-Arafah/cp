#include<stdio.h>
int factorial(int n, int i)
{
    if(n<=1) return 1;
    return n*factorial(n-i, i);
}
int main()
{
    int n, i, mul=1;
    char c, str[21];
    scanf("%d%c", &n, &c);
    gets(str);
    for(i=0; str[i]!='\0'; i++);
    printf("%d\n", factorial(n, i));
}
